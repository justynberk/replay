{
  "name": "replay",
  "version": "0.1.0-beta.1",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "app"
  ],
  "scripts": {
    "start": "npm run start --workspace @replay/local",
    "dev": "npm run dev --workspace @replay/local",
    "doctor": "npm run doctor --workspace @replay/local",
    "build": "npm run build --workspace @replay/local",
    "test": "npm run test --workspace @replay/local",
    "test:sites": "npm run test:sites --workspace @replay/local",
    "release:pack": "node scripts/package-release.mjs"
  }
}
